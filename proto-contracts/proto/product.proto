syntax = "proto3";

package product;

service ProductService {
  rpc GetStatus (Empty) returns (StatusResponse);
}

message Empty {}

message StatusResponse {
  string service = 1;
  string status = 2;
}
