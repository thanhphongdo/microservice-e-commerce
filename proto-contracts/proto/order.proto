syntax = "proto3";

package order;

service OrderService {
  rpc GetStatus (Empty) returns (StatusResponse);
}

message Empty {}

message StatusResponse {
  string service = 1;
  string status = 2;
}
